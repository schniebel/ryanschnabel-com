apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: cloudflare-tunnel
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/cloudflare/helm-charts
  ref:
    branch: main