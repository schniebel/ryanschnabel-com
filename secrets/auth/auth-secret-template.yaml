# Example to generate new username and password:
#   htpasswd -nb <username> <password> | base64

apiVersion: v1
kind: Secret
metadata:
  name: auth-secret
data:
  users: |
    ****encoded password hash here****
