name: Deployment CI

on:
  push:
    branches: [ "main" ]

jobs:

  Build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
        docker build -t schniebel/welcome-page:$(date +%s) .
