apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: auth
  namespace: dashboard
spec:
  forwardAuth:
    address: http://oauth2-proxy.oauth2-proxy.svc.cluster.local:44180/oauth2/auth
    trustForwardHeader: true
    authResponseHeaders:
      - "X-Forwarded-User"