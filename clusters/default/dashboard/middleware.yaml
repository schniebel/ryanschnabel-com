apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: oauth-auth
  namespace: oauth2-proxy
spec:
  forwardAuth:
    address: http://oauth2-proxy-service.oauth2-proxy.svc.cluster.local:44180
    authResponseHeaders:
      - X-Forwarded-User