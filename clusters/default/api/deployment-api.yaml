apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      serviceAccountName: api-service-account 
      nodeSelector:
        kubernetes.io/hostname: master
      containers:
      - name: api
        image: schniebel/ryanschnabel-api:2d9b9745d730575ba7109371459e4b9d3e706371
        ports:
        - containerPort: 8080
        env:
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: api-key-secret
              key: API_KEY
        - name: SECRET_NAME
          value: "whitelist-secret"
        - name: SECRET_NAMESPACE
          value: "traefik-forward-auth"
        - name: SECRET_DATA_KEY
          value: "whitelist"