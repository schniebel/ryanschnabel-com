apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: graf-data-openebs-pvc
  namespace: monitoring
spec:
  storageClassName: openebs-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
